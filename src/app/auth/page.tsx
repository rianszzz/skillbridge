import AuthForm from "./auth-form";
export default async function AuthPage({ searchParams }: { searchParams: Promise<{ mode?: string }> }) { const signup = (await searchParams).mode === "signup"; return <main id="main"><header className="page-head"><p className="eyebrow">Akun Skillbridge</p><h1>{signup ? "Daftar untuk mulai menilai." : "Masuk untuk menyimpan bukti."}</h1><p className="lede">Akun menjaga riwayat lintas perangkat dan mengisolasi data setiap pengguna.</p></header><AuthForm initialMode={signup ? "signup" : "login"}/></main>; }
